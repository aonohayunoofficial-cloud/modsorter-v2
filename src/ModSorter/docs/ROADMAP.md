# modsorter-v2 パラメトリック生成機能 マスターリスト & ロードマップ

最終更新: 2026-07-23
対象リポジトリ: aonohayunoofficial-cloud/modsorter-v2

---

## 0. 全体方針（確定事項）

### KPI（優先順位順）
1. **網羅性** — 中分類を妥協なく全登録する
2. **生成物の再現度** — 各中分類は実物研究に基づき実物に忠実に生成する
3. 最短性 — 上位KPIを損なわない範囲で成果を早く出す（下位）

### モード構成（トップレベル3分割）
- **手動生成**（選択式パラメトリック）
- **AI生成**（既存の簡易建築をリネーム。将来ローカルLLMのプロンプト変換生成のみ撤去。
  パイプラインは プロンプト→画像生成→3D化→ボクセル化→3Dプレビュー→.nbt出力 を温存）
- **クリエイト建築**（機能ブロック生成。今回ノータッチ。将来ロボット系へ）

### UI構造（確定）
- 手動生成タブの**上部に「大分類選択 → 中分類選択」を設置**し、選択に応じて
  下部のパラメータUI（スライダー/トグル/ブロック選択）を切り替える方式
- 大分類 ComboBox → 中分類 ComboBox → パラメータパネル（動的差し替え）

### 進め方の原則
- 既存を壊さず横付け（追加優先）。AI生成パイプラインは資産として温存
- 出力は全モード共通で `List<GeneratedBlock>` → PreviewHtml → StructureNbtWriter に流す
- 各中分類は「実物研究 → 実装」の2段構え
- LLM撤去はユーザーが「撤去」と指示したタイミングで実施

### 重要な技術的事実（コード確認済み）
- `GeneratedBlock` は現状 `X, Y, Z, Id` のみ。可動メタ用に nullable な `Properties`（ブロックステート辞書）追加が必要
- `StructureNbtWriter` は `Properties` 辞書を受け取り任意のブロックステートを書ける（axis/facing 対応可能）
- `StructureNbtWriter.Save` は負座標を扱えない。全ブロックを非負に正規化してから書く
- `StructureExpander` は決定論的展開ロジック。手動生成モードの中核資産
- `volumes`（VolumePart 合成）実装済み → 双胴船・複数構造物に活用可能
- `Genre.cs` + `GenreCatalog.cs` + `Genres/*.json` によるデータ駆動機構が既存
- 非矩形フットプリント時は屋根が flat に強制フォールバックする制約あり

---

## 1. 大分類・中分類 マスターリスト（確定版 v2）

### 大分類1: プロペラ（Propeller）── 単体オブジェクト
- 2枚羽 / 3枚羽 / 4枚羽 / 5枚以上（多翼）
- 後退翼(swept) / 直線翼 / 曲線翼
- 可変ピッチ翼 / ダクテッド(覆い付き) / 二重反転(contra-rotating)

パラメータ: 枚数・全長・根元弦長・翼端弦長・後退角・ねじり(twist/pitch)・翼形状・素材・回転角

### 大分類2: バルーン（Balloon）── 単体オブジェクト
- 葉巻型(ツェッペリン式) / 涙滴型 / 球形 / 半球(熱気球式)
- 紡錘型 / 多気嚢(連結) / ブリンプ(軟式) / 硬式飛行船(リブ骨格あり)

パラメータ: 全長・幅・高さ・テーパー・シェル厚・リブ間隔・キール深さ・フィン高/長

### 大分類3: 船体（Hull）── 単体オブジェクト（約30種）

既存資産:
- 手漕ぎボート / モーターボート / トロール船 / キャラベル / ガレオン
- 客船(liner) / 貨物船(cargo) / 駆逐艦 / 戦艦 / 空母 / 潜水艦

createmod 21種:
- Sloop / Schooner / Frigate / Galleon* / Carrack / Longship
- Clipper / Ship of the Line / Cog / Pinnace / Dhow / Junk
- War Galley / Flying Ship / Dragon Ship / Ark / Trawler* / Tugboat
- Destroyer* / Speedboat / Yacht  （* = 既存と重複、統合）

追加要望:
- 双胴船(catamaran) / 潜水母艦(submarine tender)

パラメータ: 全長・全幅・喫水深・船底絞り・船体フレア・フレアカーブ・タンブルホーム・
シアーカーブ・船首鋭さ・船尾オーバーハング ＋船種固有の上部構造

### 大分類4: 建築物（Building）── 現実建築の外観を網羅

**建物系中分類（箱ベース: 幅・奥行き・高さ・屋根形式）**
- 戸建て住宅(家) / 集合住宅(アパート/マンション) / 工場 / 研究所
- 倉庫 / 店舗・商業施設 / オフィスビル / 宗教建築(教会/神殿)
- 塔(タワー) / 記念建築(モニュメント) / 屋外イベント会場 / 公共施設

**交通インフラ系中分類（実地調査に基づく単体構造物の粒度）**
※港・空港・駅は「集合体」ではなく、それを構成する単体構造物を中分類として登録。
　将来これらを並べて配置すると結果的に港/空港/駅になる。

港湾（国交省 港湾空港部 分類準拠）:
- 岸壁 / 桟橋 / 防波堤 / 上屋(倉庫) / ドライドック
- ガントリークレーン(コンテナクレーン) / 橋形クレーン / 係船柱 / 灯台

空港（国交省 空港土木/建築施設 分類準拠）:
- 管制塔 / 旅客ターミナル / 貨物ターミナル / 格納庫(ハンガー)
- 滑走路 / 誘導路 / エプロン / 進入灯 / ヘリポート

鉄道駅:
- 駅舎 / プラットフォーム(相対式/島式) / ホーム上屋 / 跨線橋 / 車庫

橋梁:
- 桁橋 / 吊り橋 / アーチ橋 / 跳開橋

産業インフラ:
- 発電所 / 風車 / 水車 / サイロ / 給水塔 / タンク

**平面土木構造物の扱い**: 滑走路・誘導路・エプロン・桟橋・防波堤・岸壁などの
平面主体構造物も建築グループ内に登録する（ユーザー確定）。
ただしパラメータ設計は箱ベースと別系統（長さ・幅・厚み・付帯設備の有無）とする。

**共通パラメータ（建物系）**: 幅・奥行き・高さ・階数・屋根形式・屋根勾配(roof_pitch)・
軒の出(eave_overhang)・軒面選択(N/S/E/W)・開口配置・煙突(本数/高さ/太さ/貫通/寄せ)

**屋根形式マスター（Minecraft wiki 準拠）**:
- 平屋根[済] / 片流れ(shed)[済・要修正] / 切妻(gable)[済] / 切妻階段(gable_stairs)[部]
- 寄棟(hip) / 半寄棟 / サルトボックス / ダッチゲーブル / ギャンブレル / マンサード
- ヘルム(塔用) / 四角錐ピラミッド(pyramid)[部・UI未接続] / ドーム(dome)[部]
- 鋸屋根(sawtooth・工場) / モニター屋根(monitor・工場)

規模: 大分類4 / 中分類 プロペラ10・バルーン8・船体約30・建築（建物系12＋交通インフラ系約32）

---

## 2. ロードマップ（フェーズ順）

### フェーズ0: 事実確定
- `ShipExpander` 船種別ビルダー末尾を読了し、船が「作り直し」か「形状パラメータ差し込み」かを確定
- 工場を建物の1バリアントとするか別Specとするかを判断
- AI生成モードの呼び出し元配線を1箇所特定

### フェーズ1: モード分岐の土台（追加のみ・既存ゼロ改変）
- `ArchitectModeHost` を3モードハブに拡張
- UIにモード切替（手動生成／AI生成／クリエイト建築）追加
- 既存の簡易建築を「AI生成」にリネーム

### フェーズ1.5: 手動生成UIの大分類→中分類セレクタ（新規）
- 手動生成タブ上部に大分類/中分類 ComboBox を設置
- 選択に応じてパラメータパネルを動的差し替えする土台を作る

### フェーズ2: 出力の器の拡張（改修）
- `GeneratedBlock` に nullable な `Properties`（ブロックステート辞書）追加
- PreviewHtml が Properties を無視しても壊れないことを確認

### フェーズ3: 建築物(家) 縦通し ＋ 屋根積み残し解消（進行中）
- 既存パラメータ（箱・屋根・開口・柱・軒・煙突）で一本通す
- **片流れ屋根の妻側開口を妻壁立ち上げで塞ぐ（方式①・5審議で確定済み）**
- **ピラミッド屋根をUI屋根タイプに接続（BuildPyramidRoof は実装済み）**

### フェーズ4: 建築物 中分類の網羅実装（新築＋改修）
- 建物系（工場・集合住宅・研究所・倉庫・店舗・オフィス・宗教・塔・記念・イベント・公共）
- 交通インフラ系（港湾・空港・駅・橋梁・産業インフラ）を単体構造物粒度で実装
- 屋根形式追加（hip/sawtooth/monitor 等）・大型シャッター・平面土木パラメータ系統

### フェーズ5: 手動生成モード・船体（パラメトリック hull）
### フェーズ6: 船種の網羅実装（約30種）
### フェーズ7: プロペラ・バルーン（パラメトリック自前実装）
### フェーズ8: AI生成のLLM撤去（ユーザー指示時）

### 将来（今回スコープ外）
- クリエイト建築 / ロボット（二足歩行・可動メタ本領）

---

## 3. 各フェーズ共通の完了条件
- 実物（写真/資料）と生成物を突き合わせ再現度確認（KPI2）
- 中分類はマスターに全登録、未実装はUIで明示（KPI1）
- 既存モード（AI生成）が壊れていないことを確認

---

## 4. 未確定・要判断事項
- 船体: 既存 ShipExpander を活かすか作り直すか（フェーズ0）
- 工場: 建物バリアントか別Specか（フェーズ0）
- 中分類のデータ構造: 既存 Genre(JSON)拡張か新テーブルか
- 平面土木×パラメータ系統: 箱ベースと別UIをどう共存させるか
- 非矩形フットプリント×勾配屋根の制約
