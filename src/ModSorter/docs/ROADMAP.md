# modsorter-v2 パラメトリック生成機能 マスターリスト & ロードマップ

最終更新: 2026-08-19
対象リポジトリ: aonohayunoofficial-cloud/modsorter-v2

---

## 0. 全体方針（確定事項）

### KPI（優先順位順）
1. **網羅性** — 中分類を妥協なく全登録する
2. **生成物の再現度** — 各中分類は実物研究に基づき実物に忠実に生成する
3. 最短性 — 上位KPIを損なわない範囲で成果を早く出す（下位）

### モード構成（トップレベル3分割）
- **手動生成**（選択式パラメトリック）
- **AI生成**（既存の簡易建築をリネーム。将来ローカルLLMのプロンプト変換生成のみ撤去。
  パイプラインは プロンプト→画像生成→3D化→ボクセル化→3Dプレビュー→.nbt出力 を温存）
- **クリエイト建築**（機能ブロック生成。今回ノータッチ。将来ロボット系へ）

### UI構造（確定・v3で更新）
- 手動生成タブの**上部に「大分類選択 → 中分類選択 → 小分類選択」を設置**し、選択に応じて
  下部のパラメータUI（スライダー/トグル/ブロック選択）を切り替える方式
- 大分類 ComboBox → 中分類 ComboBox → 小分類 ComboBox → パラメータパネル（動的差し替え）
- **v3変更点**: v2 までは大分類直下に中分類が最大45件並び、目的の項目までスクロールが必要だった。
  「港湾」「空港」のような下位グルーピングを正式な階層（中分類）へ昇格させ、
  従来の中分類を小分類に降格。どの階層でも一覧が最大12件に収まる
- 呼称の対応: v2「中分類」＝v3「小分類」。v2 の `Group` フィールド＝v3 の中分類

### 進め方の原則
- 既存を壊さず横付け（追加優先）。AI生成パイプラインは資産として温存
- 出力は全モード共通で `List<GeneratedBlock>` → PreviewHtml → StructureNbtWriter に流す
- 各小分類は「実物研究 → 実装」の2段構え
- LLM撤去はユーザーが「撤去」と指示したタイミングで実施

### 重要な技術的事実（コード確認済み）
- `GeneratedBlock` に nullable な `Properties`（ブロックステート辞書）を追加済み【フェーズ2完了】。
  null または空なら従来の単一立方体として扱われる（後方互換）
- `StructureNbtWriter` は `Properties` 辞書を受け取り任意のブロックステートを書ける（axis/facing 対応可能）
- `StructureNbtWriter.Save` は負座標を扱えない。全ブロックを非負に正規化してから書く
- `StructureExpander` は決定論的展開ロジック。手動生成モードの中核資産
- `StructureExpander.Civil.cs` が土木系（スロープ・橋）の別系統として稼働中。
  床/壁/屋根/開口部を通さず座標リストを直接返す。平面土木はこの系統に倣う
- `volumes`（VolumePart 合成）実装済み → 双胴船・複数構造物に活用可能
- `Genre.cs` + `GenreCatalog.cs` + `Genres/*.json` によるデータ駆動機構が既存
- 非矩形フットプリント時は屋根が flat に強制フォールバックする制約あり
- **分類の単一の正は `src/ModSorter/Architect/Manual/ManualCatalog.cs`**。
  この文書はその写しと進捗管理を担う。片方だけ更新しないこと

---

## 1. 大分類・中分類・小分類 マスターリスト（確定版 v3）

`[x]` = 実装済み（`ManualCatalog` に factory あり）、`[ ]` = 未実装（UIに「（未実装）」と表示）。
括弧内は `ManualCatalog` の Id。

### 進捗サマリ

| 大分類 | 中分類 | 小分類 | 実装済み |
|---|---:|---:|---:|
| 建築物 | 7 | 50 | 50 |
| 船体 | 8 | 31 | 0 |
| プロペラ | 3 | 10 | 0 |
| バルーン | 2 | 8 | 0 |
| **合計** | **20** | **99** | **50** |

---

### 大分類1: プロペラ（`propeller`）── 単体オブジェクト

- 中分類「枚数」(`blade_count`) 0/4:
  [ ] 2枚羽(`blade2`) / [ ] 3枚羽(`blade3`) / [ ] 4枚羽(`blade4`) / [ ] 5枚以上・多翼(`blade_multi`)
- 中分類「翼形状」(`blade_shape`) 0/3:
  [ ] 後退翼(`swept`) / [ ] 直線翼(`straight`) / [ ] 曲線翼(`curved`)
- 中分類「機構」(`blade_mech`) 0/3:
  [ ] 可変ピッチ翼(`variable_pitch`) / [ ] ダクテッド・覆い付き(`ducted`) / [ ] 二重反転(`contra_rotating`)

パラメータ: 枚数・全長・根元弦長・翼端弦長・後退角・ねじり(twist/pitch)・翼形状・素材・回転角

### 大分類2: バルーン（`balloon`）── 単体オブジェクト

- 中分類「形状」(`envelope_shape`) 0/6:
  [ ] 葉巻型・ツェッペリン式(`cigar`) / [ ] 涙滴型(`teardrop`) / [ ] 球形(`sphere`) /
  [ ] 半球・熱気球式(`hemisphere`) / [ ] 紡錘型(`spindle`) / [ ] 多気嚢・連結(`multi_cell`)
- 中分類「構造形式」(`envelope_frame`) 0/2:
  [ ] ブリンプ・軟式(`blimp`) / [ ] 硬式飛行船・リブ骨格あり(`rigid`)

パラメータ: 全長・幅・高さ・テーパー・シェル厚・リブ間隔・キール深さ・フィン高/長

### 大分類3: 船体（`hull`）── 単体オブジェクト（31種）

中分類は用途・帆走/機走・時代で切る。既存資産（`ShipExpander`）11種と
createmod 21種、追加要望2種を統合し重複を解消した結果が下記31種。

- 中分類「小型艇」(`small_craft`) 0/5:
  [ ] 手漕ぎボート(`rowboat`) / [ ] モーターボート(`motorboat`) / [ ] スピードボート(`speedboat`) /
  [ ] ヨット(`yacht`) / [ ] 双胴船(`catamaran`)
- 中分類「作業船」(`work_boat`) 0/2:
  [ ] タグボート(`tugboat`) / [ ] トロール船(`trawler`)
- 中分類「帆船・中世〜大航海」(`sail_old`) 0/8:
  [ ] コグ船(`cog`) / [ ] ロングシップ(`longship`) / [ ] ダウ船(`dhow`) / [ ] ジャンク船(`junk`) /
  [ ] ピナス(`pinnace`) / [ ] キャラベル(`caravel`) / [ ] キャラック(`carrack`) / [ ] ガレオン(`galleon`)
- 中分類「帆船・近代」(`sail_modern`) 0/3:
  [ ] スループ(`sloop`) / [ ] スクーナー(`schooner`) / [ ] クリッパー(`clipper`)
- 中分類「帆走軍艦」(`warship_sail`) 0/3:
  [ ] フリゲート(`frigate`) / [ ] 戦列艦(`ship_of_the_line`) / [ ] 軍用ガレー(`war_galley`)
- 中分類「商船」(`merchant`) 0/2:
  [ ] 客船(`liner`) / [ ] 貨物船(`cargo`)
- 中分類「近代軍艦」(`warship_modern`) 0/5:
  [ ] 駆逐艦(`destroyer`) / [ ] 戦艦(`battleship`) / [ ] 空母(`carrier`) /
  [ ] 潜水艦(`submarine`) / [ ] 潜水母艦(`submarine_tender`)
- 中分類「特殊・空想」(`fantasy`) 0/3:
  [ ] 方舟(`ark`) / [ ] 飛行船体(`flying_ship`) / [ ] ドラゴンシップ(`dragon_ship`)

パラメータ: 全長・全幅・喫水深・船底絞り・船体フレア・フレアカーブ・タンブルホーム・
シアーカーブ・船首鋭さ・船尾オーバーハング ＋船種固有の上部構造

### 大分類4: 建築物（`building`）── 現実建築の外観を網羅（50種）

#### 中分類「建物」(`bldg`) 12/12 ── 箱ベース（幅・奥行き・高さ・屋根形式）

- [x] 戸建て住宅(`house`) / [x] 集合住宅(`apartment`) / [x] 工場(`factory`) / [x] 研究所(`laboratory`)
- [x] 倉庫(`warehouse`) / [x] 店舗・商業施設(`shop`) / [x] オフィスビル(`office`) / [x] 宗教建築(`religious`)
- [x] 塔(`tower`) / [x] 記念建築(`monument`) / [x] 屋外イベント会場(`event_venue`) / [x] 公共施設(`public_facility`)

#### 交通インフラ系（実地調査に基づく単体構造物の粒度）

※港・空港・駅は「集合体」ではなく、それを構成する単体構造物を小分類として登録。
　将来これらを並べて配置すると結果的に港/空港/駅になる。

**中分類「港湾」(`harbor`) 9/9** ── 国交省 港湾空港部 分類準拠

- [x] 岸壁(`quay`) / [x] 桟橋(`pier`) / [x] 防波堤(`breakwater`) / [x] 上屋・倉庫(`transit_shed`)
- [x] ドライドック(`drydock`) / [x] ガントリークレーン・コンテナクレーン(`gantry_crane`)
- [x] 橋形クレーン(`bridge_crane`) / [x] 係船柱(`bollard`) / [x] 灯台(`lighthouse`)

**中分類「空港」(`airport`) 9/9 完了** ── 国交省 空港土木/建築施設 分類準拠

- [x] 管制塔(`control_tower`) — 管制室22〜130㎡、窓は鉛直から外へ15度、外周キャットウォーク
- [x] 旅客ターミナル(`passenger_terminal`) — ゲート間隔＝エプロンのスポット幅、奥行き26〜30m、
      2層、アプロンドライブ式搭乗橋（伸長15〜45m・勾配10%以下）
- [x] 貨物ターミナル(`cargo_terminal`) — 約155㎡に1台のトラックドック、ドック高さ1.2m、
      庫内有効高さ7〜12m、エアサイド大型扉、2層の事務所棟
- [x] 格納庫・ハンガー(`hangar`) — 扉幅＝翼幅＋クリアランス、扉高さ＝尾翼高さ＋1m、
      NFPA409の区分（28ft・2026年改訂で35ft）を明示、アーチトラス・引き分け戸
- [x] 滑走路(`runway`) — コード別に幅23/45/60m、中心線・接地帯・進入端標識
- [x] 誘導路(`taxiway`) — コード別に幅10.5/18/23/25m、ショルダー込み全幅25/44/60m
- [x] エプロン(`apron`) — スポット幅＝翼幅＋クリアランス、スポット数ぶん横に並べる
- [x] 進入灯(`approach_light`) — CAT I 900m・CAT II/III 側方列270m・簡易式420m、
      灯数で距離が読める段構成、バレット時のクロスバー削減、進入灯橋、PAPI
- [x] ヘリポート(`helipad`) — D値からFATO 1D・TLOF 0.83D・セーフティ3mか0.25D、
      TD/PM円 内径0.5D、H標識、緑の縁灯 間隔5m以下、高架式

**中分類「鉄道」(`railway`) 5/5 完了**

- [x] 駅舎(`station_building`) — 地平/橋上/高架下の3形式、コンコース天井高はみなし規定
      3.0m以上（橋上の実施例5.5〜6.8m）、改札通路は機械2マス＋通路幅で並べ幅広通路は
      有効幅90cm以上、自由通路の幅員4m、エレベーターはかご内法 幅140cm×奥行135cm
      （11人乗り）に壁を足して3マス角のシャフト
- [x] プラットフォーム・単式/相対式/島式(`platform`) — 縁端は軌道中心から1.5m（実物1475mm）、
      天端はレール面上1.1m≒1マス、ホーム長＝20m車×両数、点状ブロックは縁端から1マス内側、
      ホームドアは20m車4扉＝5m間隔、ホーム端の勾配、高架（柱スパン10m級）。
      軌道は道床だけで表しレールは置かない（minecraft:rail は隣接から shape が決まる機能
      ブロックで、shape を書かない生成物では曲線状態を拾って斜めに描かれる。1マス=1m では
      軌間1067mmが1マス未満で2条にも分けられない）
- [x] ホーム上屋(`platform_canopy`) — 軒高はホーム面上4m級（駅区間は軌道から上屋まで6.5m）、
      柱間隔5m（古レール上屋は約4.5m＝5ヤード）、柱はホーム縁端の1マス内側でホーム限界
      1.80mを外す、軒を出すときは建築限界（軌道中心±1.9m・高さ5.7m）回避のため軒高6以上、
      切妻/片流れ/陸屋根/アーチ、雨とい、照明
- [x] 跨線橋(`overpass`) — 桁下はレール面上6以上（建築限界5700mm）、通路幅3m級、
      階段は蹴上げ1マス固定で踏面2＝約26.6度（実物は蹴上げ0.16m/踏面0.30m＝約28度）、
      階段幅は誘導基準140cm以上、両端/片側/なし、腰壁、屋根、中間橋脚
- [x] 車庫(`depot`) — 8両編成180m（線路長＝両数×車長＋余裕）、線路間隔5m級（本線4.0mより
      広く作業通路を取る）、有効高さ8（電車線5.00m＋懸吊0.5m＋余裕0.2m＝限界5.70m）、
      検車ピット深さ1（実物1.2m級・検車エリアの床を線路より1段低くする）、
      屋上点検ホームは車両屋根上3.6m、線路ごとの扉開口、採光帯、2階建の事務所棟

**中分類「橋梁」(`bridge`) 4/4 完了**

- [x] 桁橋(`girder_bridge`) — 桁高は支間長の1/20級（連続桁はさらに8割）、3径間連続の
      側径間:中央径間＝1:1.25、主桁間隔3m級、車線幅3.25〜3.5m、歩道2.0m級、高欄1.1m、
      照明の灯具間隔30m級、橋脚は張出式(T型)/壁式/ラーメン。単純桁は支点で桁を切って
      遊間を入れる。床版と路面は1マス=1mの丸めで2層になる（実寸は合わせて0.3m）
- [x] 吊り橋(`suspension_bridge`) — サグ比1/10級（安芸灘大橋 74.0m/750m）、側径間は
      中央径間の20〜60%（既定40%）、ハンガー間隔10〜20m級（明石海峡大橋14m）、
      主塔は門型/H型/トラス塔で床版上の高さはサグ＋余裕、補剛桁高は実橋の支間1/100級が
      1マス=1mで潰れるため最小2マス。ケーブル面と主塔は床版の外側（x=-1/W）に置くので
      車道の幅に影響しない。アンカレイジは取付部を兼ねる
- [x] アーチ橋(`arch_bridge`) — ライズ比1/5〜1/10（既定1/5）、上路式/下路式/中路式、
      上路式はリブが床版の下で支柱受け、下路式はリブが床版の外側で吊材吊り、
      タイドアーチのタイ材と横構を選択可。起拱部は推力を受けるため床版より広い基礎を置く
- [x] 跳開橋(`bascule_bridge`) — 固定トラニオン式、単葉/双葉、跳開角0〜70度
      （勝鬨橋は70秒で70度まで開く設計）、可動桁は0.25マス刻みで階段状に積む、
      釣合い錘はトラニオン後方の腕に付き開くほど橋脚内のピットへ降りる、
      機械室は主橋脚の上・路肩の外側。照明は可動桁には立てない

**中分類「産業」(`industry`) 5/5 完了**
- [x] 風車(`wind_turbine`) — 水平軸2形式＋垂直軸4形式の計6形式。
  近代（水平軸）は2,000kW級でタワー高さ78m・タワー直径4.3m（頂部は基部-2）・
  ローター直径86m、ナセルは長さ10.4m・幅3.5m・輸送時高さ4m（設置時5.4m）。
  ハブ高さ60〜100m・翼端到達高100〜150m。翼端が地面へ潜らないようローター直径は
  ハブ高さの2倍-4で自動的に抑える。
  オランダ型は下太りの塔身＋ドームのキャップ＋外周ギャラリー。羽根は縦框（whip）と
  帆桟を全ブロックで通し、帆布だけフェンスにする（従来は縁と桟だけの格子で羽根が
  透けていた）。既定のブロックは木造
  垂直軸は ダリウス（φ型）／直線翼（H型・ジャイロミル）／ヘリカル（ねじり直線翼）／
  サボニウス（S型ロータ）。ダリウスは Sandia の17m試験機（ローター直径17m・高さ21m・
  H/D≈1.24）を既定とし、翼は赤道部の直線区間（高さの15%）と半径 高さの30% の円弧を
  つないだ SLCA（Straight-Line-Circular-Arc）でトロポスキーンを近似する
  （r=R·sin(πf) の一発曲線だと赤道が尖ったレンズ形になるため差し替えた）。
  直線翼は200kW級のH型が直径26m・高さ24mで、弦長は直径の0.13を上限に取る（26m→4マス）。
  φ型の弦長はソリディティ σ≥0.08 を満たす値、サボニウスは弦1固定。主軸は直径の5%以上。
  ヘリカルは直径6m・高さ10m、サボニウスは直径6m・高さ12mで重なり比0.15。
  直径12m未満は機械室を置かず1×2の制御盤に落とす。サボニウスは3段に分けて
  360/(枚数×段数) 度ずつずらす。いずれも浮体式（スパーブイ）に
  切り替えられる。SeaTwirl の30kW機は直径10m・全高31m・水面上13m（水面下18m）、
  浮遊軸型 FAWT はロータ径9.3m・浮体径1.7m。ダリウスにはガイワイヤを付けられる
- [x] 水車(`water_wheel`) — 上掛け／胸掛け／下掛け。上掛けの実施例で水輪 直径3m・
  ブレード幅2.5m、胸掛けで直径3.0m・内幅0.4m、Laxey Wheel は直径22.1m・幅1.8m・
  バケット192個。水車小屋は五間×二間半（9m×4.5m）で軸が壁を貫いて中へ入る。
  導水路は4マスおきに支柱を川床まで落とす。水は `minecraft:water`
- [x] サイロ(`silo`) — コンクリート製の円型は直径1.5〜6m・高さは直径の2.5〜3倍。
  600tのセメントサイロはスカート支持で直径6.4m×高さ20.1m。下部ホッパー・投入シュート
- [x] 給水塔(`water_tower`) — 千葉高架水槽は内径11m・有効水深5m・容量475m³・
  満水位標高50m、中央昇降路は直径約2m。点検デッキ・航空障害灯
- [x] タンク(`tank`) — 10万バレル級で直径39m×高さ20.7m、円錐屋根の勾配1/16以下が
  放爆構造の条件（直径15m以上・高さ9m以上）。風止めリング・らせん階段（踏面2・約26.6度）・
  防油堤（高さ0.5m以上、側板からタンク高さの1/3〈直径15m未満〉または1/2〈15m以上〉）
- 昇降設備は `minecraft:ladder`、階段は階段ブロックを平ブロックと交互に置くので登れる。
  梯子と開口部は別方角を既定にして塞がないようにしている

縦型容器3件は回転体の幾何（`InR`／`Disc`／`Ring`／`Revolve`）を `IndustryExpander.cs` に
共通化し、`IndustryExpander.Vessel.cs` の3ビルダーが呼ぶ。屋根の段数は
`IndustryExpander.RoofLevels` を UI からも呼ぶので、桁橋で起きた「UIと展開側で式が
二重になる」状態を作っていない。

昇降設備は実際に登れる形にする。外部ラダーは `minecraft:ladder` を胴板の外側1マスへ
1列置き、`facing` は「取り付く壁から梯子へ向かう向き」（+z 側なら south）を入れる。
らせん階段は平ブロックと階段ブロックを交互に置き、平ブロック天端→階段の半段→
一つ上の平ブロック天端と 0.5 ずつ上がるのでジャンプなしで登れる。円周をたどる経路は
斜めに飛ぶ箇所へ直交1マスを挿し込んで直交化してから階段を置く（斜めのままだと段が
繋がらない）。`facing` は進行方向。タンクは階段が +x〜+z の弧を使うので梯子を -z 側へ逃がす。
ブロックステートは `GeneratedBlock.Properties` へ入れ、`facadeface` の回転時は座標の写像
(x,z)→(-z,x) に合わせて east→south→west→north の順に送る。

**中分類「発電所」(`power_plant`) 6/6 完了** ── 港・空港・駅と同じ粒度で、構内を単体構造物へ分解

- [x] ボイラー建屋(`boiler_house`) — 汽力の高い側の棟。既定 高さ40・作業床4層、
      煙道の張り出し、外部鉄骨、屋上まで通る点検ラダー
- [x] タービン建屋(`turbine_hall`) — 実施例で長さ60m×幅36m×高さ32m（別例 83m×83m×36m）。
      柱スパン9m、天井走行クレーン、機側の低層アネックス
- [x] 煙突(`stack`) — 新仙台火力の実施例で高さ100m・外径5.7mが2本。既定は高さ120・
      基部直径14→頂部6のテーパーに内筒2本、20mごとの踊り場、高さ60以上で航空障害灯
- [x] 冷却塔(`cooling_tower`) — 自然通風の双曲線形。実施例で全高190m・基部半径65.25m・
      喉部42m・頂部43.45mなので喉部は全高の3/4に置く。基部はレイカー柱24本、
      下部に散水盤と貯水池
- [x] 格納容器(`containment`) — PWRは直径40m級の円筒＋ドームで壁厚2・エアロック・補助建屋、
      BWRは矩形の原子炉建屋で燃料取替床と天井クレーン
- [x] 開閉所(`switchyard`) — 門型構造4基を等間隔、三相の母線、防火壁つき変圧器2台、
      フェンスと制御建屋

幾何は `IndustryExpander.Power.cs`、パラメータは `StructureSpec.Power.cs` と
`PowerPlantParamsControl.cs`。回転体は縦型容器3件と同じ `Disc`／`Ring`／`ShellDisc` を使う。

#### 平面土木構造物の扱い

滑走路・誘導路・エプロン・桟橋・防波堤・岸壁などの平面主体構造物も建築グループ内に
登録する（ユーザー確定）。パラメータ設計は箱ベースと別系統（長さ・幅・厚み・付帯設備の有無）とし、
生成も `StructureExpander.Civil.cs` に倣って専用 Expander が座標リストを直接返す
（港湾系は `HarborExpander.cs` で実施済み。空港系は `AirportExpander.cs` を新設予定）。

#### 共通パラメータ（建物系）

幅・奥行き・高さ・階数・屋根形式・屋根勾配(roof_pitch)・軒の出(eave_overhang)・
軒面選択(N/S/E/W)・開口配置・煙突(本数/高さ/太さ/貫通/寄せ)

#### 屋根形式マスター（Minecraft wiki 準拠）

- 平屋根[済] / 片流れ(shed)[済] / 切妻(gable)[済] / 切妻階段(gable_stairs)[部]
- 寄棟(hip) / 半寄棟 / サルトボックス / ダッチゲーブル / ギャンブレル / マンサード
- ヘルム(塔用) / 四角錐ピラミッド(pyramid)[済・UI接続済] / ドーム(dome)[部]
- 鋸屋根(sawtooth・工場)[済] / モニター屋根(monitor・工場)[済]

---

## 2. ロードマップ（フェーズ順）

### フェーズ0: 事実確定 ── 完了
- `ShipExpander` 船種別ビルダー末尾を読了。船は「形状パラメータ差し込み」ではなく
  共通の断面生成器を新設して作り直す方針とする（フェーズ5で着手）
- 工場は建物の1バリアント（箱ベース＋鋸屋根/モニター屋根）として実装済み
- AI生成モードの呼び出し元配線を特定済み

### フェーズ1: モード分岐の土台（追加のみ・既存ゼロ改変） ── 完了
- `ArchitectModeHost` を3モードハブに拡張
- UIにモード切替（手動生成／AI生成／クリエイト建築）追加
- 既存の簡易建築を「AI生成」にリネーム

### フェーズ1.5: 手動生成UIの分類セレクタ ── 完了（v3で3段に再改修）
- 手動生成タブ上部に大分類/中分類 ComboBox を設置し、`ManualCatalog` 駆動で流し込み
- **v3改修**: 中分類 ComboBox を挿入して3段化。
  `ManualCategoryCombo` / `ManualMiddleCategoryCombo` / `ManualSubCategoryCombo` の連鎖更新。
  既定選択は「最初の実装済みを含む中分類 → その中の最初の実装済み小分類」

### フェーズ2: 出力の器の拡張（改修） ── 完了
- `GeneratedBlock` に nullable な `Properties`（ブロックステート辞書）追加済み
- PreviewHtml が Properties を無視しても壊れないことを確認済み

### フェーズ3: 建築物(家) 縦通し ＋ 屋根積み残し解消 ── 完了
- 既存パラメータ（箱・屋根・開口・柱・軒・煙突）で一本通し済み
- 片流れ屋根の妻側開口を妻壁立ち上げで塞ぐ（方式①）── 実装済み
- ピラミッド屋根をUI屋根タイプに接続 ── 実装済み

### フェーズ4: 建築物 小分類の網羅実装 ── 完了（50/50）
- 建物系12件 ── **完了**
- 港湾9件 ── **完了**（ガントリークレーンのブーム起伏を実機準拠 0〜84度に修正済み。
  ラチスブームの実用上限70〜80度、STS の格納姿勢がほぼ上端）
- 空港9件 ── **完了**（土木3件・管制塔・旅客/貨物ターミナル・格納庫・進入灯・ヘリポート）
- 鉄道5件 ── **完了**（プラットフォーム・上屋・跨線橋・車庫・駅舎。すべて建築限界
  〈直流電化 高さ5700mm・幅3800mm〉を侵さない位置に取り付けてある）
- **橋梁4件 ── 完了。桁橋で確定した横断構成（車線／中央分離帯／区画線／地覆／歩道／
  高欄）は `BridgeExpander.Deck.cs` の `MakeSection`／`BuildDeckSurface` に共通化し、
  吊り橋・アーチ橋・跳開橋はこれを呼ぶ。桁橋だけは先に書いたため同じ割り付けを
  自前で持っている（式を直すときは両方を直す）**
- 産業5件 ── **完了**（サイロ・給水塔・タンク・風車・水車。
  回転体は canonical で回転面を x-y 平面に置き、facade_face で回す。
  `IndustryExpander.Face` の east/west の取り違えを修正済み）
- 発電所6件 ── **完了**（ボイラー建屋・タービン建屋・煙突・冷却塔・格納容器・開閉所。
  発電所は1つの箱で表せないため、港・空港・駅と同じく単体構造物を小分類として登録した）
- 屋根形式追加（hip/ギャンブレル/マンサード/ヘルム 等）・大型シャッターは
  小分類の網羅とは別枠の継続項目として残す

### フェーズ5: 手動生成モード・船体（パラメトリック hull）
共通の断面生成器を先に作る。竜骨→フレーム→外板→甲板→上部構造の順。
帆装（マスト・ヤード・索具）は帆船系の共通モジュールとして切り出す。

- 断面生成器 ── 実装済み。`HullExpander.cs` / `HullExpander.Form.cs` /
  `HullExpander.Shell.cs` と `StructureSpec.Hull.cs`。structure_type は "hull:<船種>" で、
  AI生成側の "ship"（ShipExpander）とは接頭辞もプロパティ（ship_* と hull_*）も分ける。
  素材スロットは共通のもの（hull_block/deck_block/base_block/accent_block/parapet_block）を
  流用し、二重宣言を作らない。
- 船型は `HullExpander.Form` の1か所に集約し、外寸は `HullExpander.Extent(spec)` を
  UI からも呼ぶ。桁橋で起きた「UIと展開側で式が二重になる」状態を作らない。
- 断面は超楕円 (|x|/b)^k + ((喫水線-y)/(喫水線-船底))^k = 1。k=1 が直線V（デッドライズの
  深い滑走艇）、k=2 が円弧（丸ビルジの排水量型）、k=8 がほぼ矩形（Cm≈0.98 のタンカー）。
- 水線の平面形は 船首テーパー／平行部／船尾の絞り の3区間。船首テーパー長＝最大半幅÷
  tan(入角)。入角（水線の半角）は実船8〜20度、肥えた船で30度超。
- シアは ICLL 1966 の標準シア。船尾垂線 25(L/3+10) mm・L/6 で11.1・L/3 で2.8・船体中央 0・
  前方 L/3 で5.6・L/6 で22.2・船首垂線 50(L/3+10) mm で、船首側が船尾側の2倍。
  倍率100%が標準で、反りの強いロングシップは200〜300%を指定する。
- 船首材の傾斜は現代の直立船首0〜10度・快速帆船45度超。水平方向の走り＝深さ×tan(傾斜)
  だけ船底の前端が甲板の前端より後ろへ下がる。
- 外板は体積の境界抽出で置く。面ごとに条件を書き分けると船首テーパーや船尾の
  立ち上がりの斜面に穴が空くため、内外判定から拾って必ず閉じるようにした。
- フレーム間隔は実船0.5〜0.9m級だが1マス=1mでは表現できないので、見えるように
  2マス以上へ丸める。ブルワークは満載喫水線規則の1m以上を最小とする。
- 残り: 上部構造（船楼・操舵室）の共通モジュール、帆装モジュール、
  `HullParamsControl` と `ManualCatalog` の配線。配線するまで "hull:" を投げる
  呼び出し側は無く、既存の生成物は一切変わらない。

### フェーズ6: 船種の網羅実装（31種）
### フェーズ7: プロペラ・バルーン（パラメトリック自前実装）
回転体・曲面の生成器が要る。バルーンは楕円体シェルの近似が中心。
### フェーズ8: AI生成のLLM撤去（ユーザー指示時）

### 将来（今回スコープ外）
- クリエイト建築 / ロボット（二足歩行・可動メタ本領）

---

## 3. 各フェーズ共通の完了条件
- 実物（写真/資料）と生成物を突き合わせ再現度確認（KPI2）
- 小分類はマスターに全登録、未実装はUIで明示（KPI1）
- 既存モード（AI生成）が壊れていないことを確認
- `ManualCatalog.cs` と本文書の両方を同じコミットで更新する

## 3.1 小分類1件あたりの実装手順
1. `StructureSpec.cs` に必要なプロパティを追加（既存で足りるなら追加しない）
2. 生成ロジックを中分類ごとの Expander に書く（例: `HarborExpander.cs` / `AirportExpander.cs`）
3. `StructureExpander.cs` に `XxxExpander.Handles(structureType)` の分岐を足す
4. `Manual/` に `IManualParamControl` 実装の UserControl を追加。
   パラメータが近い小分類は kind 引数で1クラスに束ねる（例: `CraneParamsControl("gantry")`）
5. `ManualCatalog.cs` の該当行を `Todo` から `Impl` に変える
6. 本文書の該当行を `[ ]` → `[x]` に変え、進捗サマリの数字を直す
7. プレビューで実物写真と見比べ、骨格が成立しているか確認してから push

## 3.2 寸法データの扱い
実在の構造物は実寸を調べてから作る。当てずっぽうの比率で作らない。
出典と数値はパラメータUIの注記か Expander のコメントに残す。

収集済み: ガントリークレーンのブーム起伏角0〜84度（実用上限70〜80度）／
滑走路 幅45m級・中心線・接地帯標識・進入端標識／誘導路 幅23m以上・ショルダー9.5m／
エプロン スポット単位の駐機区画／
管制室の床面積 234・350・625・850 sq ft＝22・33・58・79㎡（FAA Order 6480.7D）、
羽田の新管制塔は約130㎡・塔高113m級／管制室の窓は鉛直から外へ15度／
管制室の平面形は正方形・五角形・六角形・八角形・円形（八角形が最多）／
管制室の外周に窓清掃用のキャットウォーク（幅1m級）／
ゲート1つあたりの桁行きは平均33〜40m、ピア全長210〜300m（FAA AC 150/5360-13）／
ダブルローデッドの動線幅30ft（約9m・動く歩道なし）、ラウンジ奥行き25〜30ft（8〜9m）／
出発が上階・到着が下階の2層構成／
アプロンドライブ式搭乗橋は伸長15〜45m、ロタンダ高さ5m級（最大8m）、勾配10%以下／
トラックドックは建物床面積1,000sq ftあたり0.6台（旧0.3台）＝約155㎡に1台、
扉は幅9ft・高さ10ft、ドック高さ48インチ（1.2m）、建物の面から取付道路まで150ft（約46m）／
庫内有効高さは22ft（約7m）が従来標準・自動段積みで40ft（約12m）／
事務所は倉庫面積の10%／エプロンは建物床面積の4.5倍、敷地は建物15%・ランドサイド25%・
エアサイド60%／貨物機の所要ランプはコードC 2,300平方ヤード・D 3,900・E 6,500・F 8,650／
格納庫の扉高さはNFPA409で28ft（8.5m）超がGroupI・2026年改訂で境が35ft（10.7m）へ／
尾翼高さはCRJ200 6.2m・A320 11.8m・B777 18.5m・A380 24.1m、扉高さは尾翼＋1〜1.5m／
A380対応格納庫の実例は幅45m×奥行62m×有効高さ18m／
進入灯CAT Iはセンターライン900m・間隔30m、クロスバー150/300/450/600/750m、
300mのものが長さ30mで他は外縁が進入端の300m先で収束、0〜300m:1灯・300〜600m:2灯・
600〜900m:3灯／CAT II/IIIは270mまで赤の側方列／簡易式は420m・間隔60m・300mに
長さ18mか30mのクロスバー1本／バレットは簡易式3m以上・他4m以上で、使うとクロスバーは
CAT I 1本・CAT II/III 2本に減る／
ヘリポートはD値基準でFATO 1D（限定用途0.83D）・TLOF 0.83D・
セーフティエリアは3mか0.25Dの大きい方、TD/PM円は内径0.5D、
H標識はD<16mで高さ3m、TLOF縁灯は緑・間隔5m／
サイロはコンクリート製の円型で直径1.5〜6m・高さ5〜18m（直径の2.5〜3倍）、
600tのセメントサイロはスカート支持で直径6.4m×高さ20.1m／
給水塔（千葉県水道局 千葉高架水槽）は内径11m・有効水深5m・容量475m³・満水位標高50m、
中央の昇降路は直径約2m／
屋外貯蔵タンクは10万バレル級で直径39m×高さ20.7m、円錐屋根の勾配はtanθ=1/16以下が
放爆構造の条件（直径15m以上・高さ9m以上に適用）／
防油堤は高さ0.5m以上・容量はタンク容量の110%以上、タンク側板から堤までは
直径15m未満でタンク高さの1/3以上・15m以上で1/2以上、堤1つにつきタンク10基まで
ホーム高さはレール面上1100mm（電車専用）・920mm（電車列車共用）・1250mm（新幹線）／
ホーム縁端は軌道中心から1475mm（JR在来線）／ホーム幅の下限は島式3.0m・相対式2.0m、
通勤駅の実際は5〜10m級／縁端警告の点状ブロックは縁端から80cm以上離す、
島式で向かい合う内方線の中心間は60cm以上／ホーム長は20m車×両数で10両200m強／
軌道中心間隔は在来線4.0m・新幹線4.3m／建築限界は直流電化で高さ5700mm・幅3800mm
（軌道中心から左右1900mm、レール面上4300mmが基礎限界）／軌間1067mm・1435mm／
ラーメン高架橋の柱スパンは8〜10m級／駅の階段は蹴上げ16cm以下・踏面30cm以上・幅140cm以上／
桁橋の適用支間は25〜150m（世界最大支間300m）／桁高は支間長の1/20級で連続桁はさらに低く
できる／3径間連続桁の側径間:中央径間＝1:1.25 が最も鋼重が軽い（土木学会 鋼連続合成桁の
設計法に関する検討）／主桁間隔3m級／床版厚0.22m・舗装厚0.08m／車線幅3.25〜3.5m／
歩道幅2.0m級で車道との段差（地覆）0.15〜0.25m／高欄1.1m／区画線の幅0.15m／
道路照明の灯具間隔30m級（道路照明施設設置基準、橋長50m以上で連続照明）／
橋脚は張出式（T型）が最も一般的、幅員が広い橋では壁式
サグ比1/10前後（安芸灘大橋 サグ74.0m・中央支間750m）／ハンガー間隔10〜20m級
（明石海峡大橋14m）／アーチのライズ比は支間の1/5〜1/10／タイドアーチの適用支間
50〜170m・世界最大は支間550m（上海盧浦大橋）／跳開橋は70秒で70度まで開く設計（勝鬨橋）

---

## 4. 未確定・要判断事項

**解決済み**
- ~~船体: 既存 ShipExpander を活かすか作り直すか~~ → 共通断面生成器を新設（フェーズ0で決定）
- ~~工場: 建物バリアントか別Specか~~ → 建物バリアント（実装済み）
- ~~中分類のデータ構造: 既存 Genre(JSON)拡張か新テーブルか~~ → 新テーブル `ManualCatalog.cs`。
  Genre(JSON) はAI生成側の資産として据え置き
- ~~平面土木×パラメータ系統: 箱ベースと別UIをどう共存させるか~~ → 専用 Expander ＋
  専用 UserControl。`IManualParamControl` の口だけ揃えれば共存する（港湾系で実証済み）

**未解決**
- 非矩形フットプリント×勾配屋根の制約（現状 flat へ強制フォールバック）
- 可動メタ（`Properties` の axis/facing）を跳開橋・風車・水車でどこまで使うか
- 空港ターミナル系（旅客/貨物）を建物系の箱ベースに寄せるか、専用Specにするか
